num = _{ "S"* ~ "0" }
var = _{ 'a'..'z' ~ "'"* }
arith = _{ "+" | "*" }
expr = { num | var | "S" ~ expr | "(" ~ expr ~ arith ~ expr ~ ")" }
quant = { "~"* ~ ("A" | "E") ~ var ~ ":" }
logical = _{ "&" | "|" | ">" }
simple = _{ expr ~ "=" ~ expr }
formula = { simple | quant ~ formula | "[" ~ formula ~ logical ~ formula ~ "]" }